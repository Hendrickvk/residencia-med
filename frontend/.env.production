# Caminho relativo: front-end e API atrás do mesmo host:porta via Caddy
# (deploy/Caddyfile, MIGRACAO.md §1) — sem isso o build de produção tentaria
# falar com http://localhost:8000, que não existe fora da máquina de dev.
VITE_API_URL=/api

# Publicação por IP (sem domínio ainda, decisão de 2026-09-12) — Streamlit
# fica numa porta própria em vez de subdomínio. Quando houver domínio,
# trocar para https://admin.SEUDOMINIO.com (ver deploy/Caddyfile.com-dominio.example).
VITE_STREAMLIT_URL=http://64.181.167.174:8080
